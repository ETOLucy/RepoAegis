# RepoAegis 架构记忆文档

> **用途**：防止上下文丢失。每次重新对话时可通过读取此文档恢复完整架构上下文。
> **最后更新**：2026-08-27
> **项目根目录**：`D:\Repos\Agents\RepoAegis`

---

## 一、18 种 SearchKind 完整映射表

| SearchKind | 主搜 QueryKind | 副搜 QueryKind | 启用 Reranker | 最大重试 |
|---|---|---|---|---|
| `exact` | LEXICAL, BM25 | BM25 | 否 | 1 |
| `path` | LEXICAL, BM25 | BM25 | 否 | 1 |
| `symbol` | SYMBOL, BM25 | BM25, VECTOR | 是 | 1 |
| `error` | LEXICAL, BM25 | BM25 | 否 | 1 |
| `history` | HISTORY, BM25 | BM25 | 否 | 1 |
| `explore` | VECTOR, BM25 | BM25, VECTOR | 是 | 2 |
| `definition` | SYMBOL, BM25 | BM25, VECTOR | 是 | 1 |
| `test` | LEXICAL, BM25 | BM25, VECTOR | 否 | 1 |
| `config` | LEXICAL, BM25 | BM25 | 否 | 1 |
| `dependency` | LEXICAL, BM25, SYMBOL | BM25 | 否 | 1 |
| `regex` | LEXICAL, BM25 | BM25 | 否 | 1 |
| `schema` | SYMBOL, BM25, VECTOR | BM25, VECTOR | 是 | 1 |
| `general` | BM25, VECTOR, OPENSEARCH | BM25, VECTOR | 是 | 2 |
| `performance` | BM25, VECTOR | BM25, VECTOR | 是 | 2 |
| `security` | LEXICAL, BM25 | BM25 | 否 | 1 |
| `api` | SYMBOL, BM25 | BM25, VECTOR | 是 | 1 |
| `ui` | LEXICAL, BM25 | BM25 | 否 | 1 |
| `ci_cd` | LEXICAL, BM25, HISTORY | BM25 | 否 | 1 |

**来源**：`src/repo_maintenance_agent/search/kind_mapping.py` → `KIND_TO_STRATEGY`

---

## 二、搜索器类型（QueryKind）

QueryKind 是"供给侧"分类，每个枚举值对应一个 `SearchPort` 实现。

| QueryKind | 搜索器 | 实现类 | 说明 |
|---|---|---|---|
| `LEXICAL` | 精确子串匹配 | `LocalLexicalSearch`（ripgrep 实现） | 精确词汇匹配，适合标识符、错误字符串 |
| `BM25` | 全文检索 | `BM25Search` | 基于词频-逆文档频率的通用全文搜索 |
| `VECTOR` | 向量相似度搜索 | `VectorSearch` | 基于 embedding 的语义搜索 |
| `SYMBOL` | 符号搜索 | `SymbolSearch` | AST 解析类名/函数名/方法名 |
| `HISTORY` | Git 历史搜索 | `GitHistorySearch` | git blame/log 分析 |
| `OPENSEARCH` | OpenSearch 全文搜索 | `OpenSearchHybridAdapter` | 仅 GENERAL 策略使用 |

**设计原则**：
- 每个 SearchKind 映射到一组**主搜 QueryKind**（精准检索）
- 副搜始终是 BM25（+VECTOR 如果可用）作为安全网
- 主搜和副搜**并行执行**，结果通过 **RRF 融合**
- 映射表是显式的、可维护的，修改一个 kind 不影响其他 kind

**来源**：`src/repo_maintenance_agent/search/router.py` → `QueryKind` 枚举

---

## 三、架构决策记录

### 3.1 search_hints 和 key_paths 已移除

- `search_hints` 和 `key_paths` 已从 `TaskSpecOutput` 中移除，避免冗余
- 搜索方向由 Research 阶段的 Rewriter（LLM 版或规则版）直接生成查询
- `TaskSpecOutput` 仅保留 `task_type`, `summary`, `acceptance_criteria`, `constraints`, `unknowns`, `calibration`
- 验证：`src/repo_maintenance_agent/agents/schemas.py` → `TaskSpecOutput`

### 3.2 CalibrationJudge 三阶段校准

- `CalibrationJudge` 在 research/planning/coding 三个阶段各校准一次
- 每个阶段独立触发，将校准结果写入 `task_spec.calibration` 字典
- 校准基于显式证据，不是 LLM 猜测
- 规则版校准规则：
  1. 证据包含测试文件但 task_type 不是 "test" → 建议改为 "test"
  2. 证据包含错误消息但 task_type 不是 "bugfix" → 建议改为 "bugfix"
  3. 证据为空 → 标记 "insufficient evidence"
- 验证：`src/repo_maintenance_agent/agents/calibration.py`

### 3.3 方案 C 重试：结果稀疏回退

- **触发条件**：搜索结果 < 3 条
- **回退策略**：切换到 GENERAL 策略重新搜索
- **最大重试次数**：3 次
- **GENERAL 不再回退**：如果当前已经是 GENERAL，不再进一步回退
- 验证：`src/repo_maintenance_agent/search/service.py` → `HybridSearchService.search()` 中的 `while len(fused) < 3` 循环

### 3.4 RRF 融合：主搜优先，副搜兜底

- 主搜结果优先，副搜结果用于填补空缺
- 使用 `Reciprocal Rank Fusion (RRF)` 算法
- 默认 `rank_constant = 60`
- 当多个检索器在同一策略内并行时，结果也通过 RRF 融合
- 验证：`src/repo_maintenance_agent/search/fusion.py` → `reciprocal_rank_fusion()`

### 3.5 9 节点流水线

```
intake → research → planning → approval → coding → verification → review → pr → finalize
```

- **intake**: 将 GitHub issue 转为结构化 `TaskSpecOutput`
- **research**: Rewriter 生成搜索查询 → HybridSearchService 检索 → Localizer（Planner+Explorer）定位代码
- **planning**: 生成 `PlanOutput`（步骤、风险等级、风险原因）
- **approval**: 审批信封机制（canonical JSON + SHA-256 密封），验证完整性
- **coding**: 生成 `PatchProposal`，执行 patch
- **verification**: 运行测试/验证，决定通过/重试/失败
- **review**: 代码审查，决定 approve/request_changes
- **pr**: 创建 Pull Request
- **finalize**: 标记 COMPLETED

验证：`src/repo_maintenance_agent/graph/builder.py` → `build_graph()`
节点实现：`src/repo_maintenance_agent/agents/nodes.py` → `build_agent_nodes()`

### 3.6 审批信封：canonical JSON + SHA-256 密封

- `ApprovalEnvelope` 包含：plan, target_commit, allowed_tools, declared_files, verification_plan
- `digest()` 方法：`json.dumps(payload, sort_keys=True, separators=(",", ":"))` → `SHA-256`
- `ApprovalDecision` 包含：plan_hash, target_commit, allowed_tools, reason, decided_at
- 验证完整性：比较 `plan_hash` 与 `ApprovalEnvelope.digest()`
- 验证：`src/repo_maintenance_agent/domain/models.py` → `ApprovalEnvelope`, `ApprovalDecision`

### 3.7 11 状态状态机

```
PENDING → INTAKE → RESEARCHING → PLANNING → NEEDS_APPROVAL → APPROVED → CODING → VERIFYING → REVIEWING → DELIVERING → COMPLETED
                                                                                                                     FAILED
                                                                                                                   CANCELLED
```

完整状态定义（`TaskStatus` 枚举）：
1. `PENDING` — 初始状态
2. `INTAKE` — 任务解析中
3. `RESEARCH` — 代码搜索中
4. `PLANNING` — 计划生成中
5. `NEEDS_APPROVAL` — 等待审批
6. `CODING` — 代码修改中
7. `VERIFYING` — 验证中（失败可回到 CODING）
8. `REVIEWING` — 审查中（失败可回到 CODING）
9. `DELIVERING` — 交付中（PR 创建）
10. `COMPLETED` — 完成
11. `FAILED` — 失败
12. `CANCELLED` — 取消

状态转换验证：`src/repo_maintenance_agent/domain/models.py` → `_TRANSITIONS`

路由逻辑：
- `route_entry`: PENDING → intake, CODING → code, 否则 → failure
- `route_after_planning`: NEEDS_APPROVAL → approval, FAILED → failure, 否则 → code
- `route_after_verification`: passed → review, CODE 错误且 iteration < max_iterations → code, 否则 → failure
- `route_after_approval`: CODING → code, 否则 → failure
- `route_after_review`: approve → pr, request_changes + iteration < max_iterations → code, 证据驱动 fallback → pr, 否则 → failure

验证：`src/repo_maintenance_agent/graph/routes.py`

---

## 四、文件路径参考

### Agent 层
| 文件 | 用途 |
|---|---|
| `src/repo_maintenance_agent/agents/nodes.py` | 9 节点流水线（intake→research→planning→approval→coding→verification→review→pr→failure） |
| `src/repo_maintenance_agent/agents/query_rewriter.py` | LLM Rewriter（LLM + 规则版 fallback） |
| `src/repo_maintenance_agent/agents/schemas.py` | TaskSpecOutput, CalibrationOutput, PlanOutput, PatchProposal, ReviewOutput, PullRequestDraft |
| `src/repo_maintenance_agent/agents/calibration.py` | CalibrationJudge（research/planning/coding 三阶段校准） |
| `src/repo_maintenance_agent/agents/localizer.py` | Localizer（Planner + Explorer 定位循环） |
| `src/repo_maintenance_agent/agents/patches.py` | Patch 渲染 |

### 搜索层
| 文件 | 用途 |
|---|---|
| `src/repo_maintenance_agent/search/kind_mapping.py` | 18 种 SearchKind → SearchStrategy 映射表 |
| `src/repo_maintenance_agent/search/service.py` | HybridSearchService（主副搜并行 + RRF 融合 + 方案 C 重试） |
| `src/repo_maintenance_agent/search/fusion.py` | RRF 融合算法 |
| `src/repo_maintenance_agent/search/rewriter.py` | 规则版查询拆分器（10 种正则模式检测） |
| `src/repo_maintenance_agent/search/router.py` | SearchRouter（正则 QueryKind 路由，fallback 到 kind_mapping） |
| `src/repo_maintenance_agent/search/embeddings.py` | OpenAIEmbeddingClient（embedding 生成） |
| `src/repo_maintenance_agent/search/reranker.py` | LLMReranker（LLM 重排序） |
| `src/repo_maintenance_agent/search/index.py` | BM25Search, VectorSearch, SymbolSearch, ingest_workspace |
| `src/repo_maintenance_agent/search/history.py` | GitHistorySearch（git log 搜索） |
| `src/repo_maintenance_agent/search/production.py` | 生产级搜索配置 |

### 图/状态层
| 文件 | 用途 |
|---|---|
| `src/repo_maintenance_agent/graph/builder.py` | LangGraph StateGraph 构建器（9 节点 + finalize） |
| `src/repo_maintenance_agent/graph/state.py` | GraphState（task + trace） |
| `src/repo_maintenance_agent/graph/routes.py` | 路由函数（entry, planning, verification, approval, review） |

### 领域模型层
| 文件 | 用途 |
|---|---|
| `src/repo_maintenance_agent/domain/models.py` | 核心领域模型（TaskStatus, RepoTaskState, ApprovalEnvelope, ApprovalDecision, SearchHit, SearchQuery 等） |
| `src/repo_maintenance_agent/domain/ports.py` | 端口定义（SearchPort, ArtifactStore 等） |
| `src/repo_maintenance_agent/domain/errors.py` | 错误类型 |

### 其他层
| 文件 | 用途 |
|---|---|
| `src/repo_maintenance_agent/inspect/bridge.py` | Inspect Agent 桥接 |
| `src/repo_maintenance_agent/api/` | FastAPI 后端 |
| `src/repo_maintenance_agent/sandbox/` | Docker 沙箱 |
| `src/repo_maintenance_agent/tools/` | 工具实现（search, git, github, patch, process 等） |
| `src/repo_maintenance_agent/policies/` | 策略（权限、风险、脱敏） |
| `src/repo_maintenance_agent/storage/` | 存储（SQLite, artifacts, queue, memory） |
| `web/` | Vite+React 前端 |
| `console/` | 静态 HTML 控制台 |

---

## 五、搜索架构概览

```
Issue Text
    │
    ▼
┌─────────────────────────────────────────────────┐
│  Rewriter (LLM 版 / 规则版)                      │
│  输出: List[RewrittenQuery {text, kind, key_paths}] │
└─────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────┐
│  HybridSearchService.search()                   │
│  ┌────────────────┐  ┌────────────────┐         │
│  │  Primary Search │  │ Secondary Search│        │
│  │  (按 kind 选择)  │  │ (BM25 + VECTOR) │        │
│  └───────┬────────┘  └───────┬────────┘         │
│          │ 并行执行            │                   │
│          ▼                    ▼                   │
│  ┌─────────────────────────────────────────┐     │
│  │  RRF Fusion (主搜优先, 副搜兜底)          │     │
│  └─────────────────┬───────────────────────┘     │
│                    │                               │
│                    ▼                               │
│  ┌─────────────────────────────────────────┐     │
│  │  方案 C 重试: < 3 条 → GENERAL 策略回退    │     │
│  │  (最多 3 次, GENERAL 不再回退)             │     │
│  └─────────────────────────────────────────┘     │
│                    │                               │
│                    ▼                               │
│  ┌─────────────────────────────────────────┐     │
│  │  LLMReranker (按需, 仅 enable_reranker 的│     │
│  │  kind 启用)                               │     │
│  └─────────────────────────────────────────┘     │
│                    │                               │
│                    ▼                               │
│              Final SearchHits                      │
└─────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────┐
│  Localizer (Planner + Explorer 定位循环)         │
│  搜索/读取/blame 多轮定位 → 最终证据 + 查询列表   │
└─────────────────────────────────────────────────┘
```

---

## 六、规则版 Rewriter 的 kind 检测优先级

规则版 `rewrite_queries()` 按以下优先级检测 kind（最优先 → 最不优先）：

1. **error** — 错误消息、Traceback、异常类型
2. **exact** — 精确标识符、引号内文本、点分路径
3. **history** — Git 历史、blame、why/who 问题
4. **symbol** — CamelCase 符号、类名/函数名
5. **path** — 文件路径提示
6. **test** — 测试相关
7. **config** — 配置相关
8. **dependency** — 依赖相关
9. **regex** — 正则模式
10. **schema** — Schema/模型定义
11. **explore** — 探索性问题
12. **general** — 通用 fallback（始终添加）

验证：`src/repo_maintenance_agent/search/rewriter.py` → `_detect_kind()`

---

## 七、重新对话恢复流程

每次重新对话时，按以下步骤恢复上下文：

1. 读取此文档（全面了解架构）
2. 如需最新代码细节，查看对应的源文件（见第四节的路径参考）
3. 如需查看现有设计文档，读取 `docs/` 目录下的文档
4. 如需查看现有任务状态，检查 `output/` 目录下的任务输出
