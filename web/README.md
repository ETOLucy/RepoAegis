# RepoAegis 控制台

Vue 3 + TypeScript + Vite。后端 API 见仓库根目录的 `src/repoaegis/server/`。

```bash
npm install
npm run dev          # 开发服务器
npm run lint         # ESLint
npm run type-check   # vue-tsc
npx vitest run       # 单元测试
npm run build        # 类型检查 + 生产构建
```
