# RepoAegis 改造计划

> **用途**：诊断当前系统的真实瓶颈并给出改造方案，供开发时参照，也是 `docs/interview-prep-complete.md` 的后续实战篇。
> **背景**：本文基于对源码和 `docs/evidence/` 评测数据的实际核查得出，不是空想设计。同步维护于个人规划仓库 `D:\Repos\Halo\agent_interview_project_prompt.md`。
> **最后更新**：2026-09-11

---

## 一、现状：真实数据说明了什么

> **2026-09-11 更新**：`docs/evidence/swebench-holdout-v2.json`（占位 `swebench__test-N` 数据）与 `docs/evidence/l2-generation-summary-20260811.json`（自称 placeholder）已删除，连带断言这份假数据的 `tests/unit/evaluation/test_public_swebench_evidence.py` 一并删除（已跑 `pytest tests/unit/evaluation` 确认无遗留引用，97 通过）。`docs/evidence/` 目录暂时清空，等下面"六、评测方法论"的金标准集建好后再回填真实证据。仓库选型待定（用户：先删占位数据，仓库后续再定）。

（以下为删除前的历史诊断，保留供参考）`docs/evidence/swebench-holdout-v2.json`：8 个任务里 4 个直接生成失败，剩下 4 个里 3 个 resolved，`strict_resolved_rate = 0.375`。

已具备且完成度高、**不需要重做**的部分：LangGraph StateGraph 9 节点编排、审批信封（SHA-256 防篡改）、Docker 沙箱四层加固、Redactor 递归脱敏、乐观锁并发控制、`EvaluationHarness` + Bootstrap 显著性检验。

**改造前第一件事**：`instance_id` 是 `swebench__test-0` 这种格式，疑似占位数据而非真实 SWE-bench instance——所有后续改进的证据都建立在评测数据之上，必须先换真实数据（见五）。

> **2026-09-11 追加发现**：两轮自动代码审计（死代码扫描 + 代码异味扫描）都没测出来，靠动手实现调用图导航前的走查才发现——`agents/localizer.py` 用 `agent="localizer"` 发 ToolCall，但 `policies/permissions.py` 的 `_AGENT_PERMISSIONS` 只认识 research/planning/coding 等 9 个 agent 名，不认识 `"localizer"`。结果是：真实运行中，Localizer 只要做非 finish 的动作（search/read/blame）就会被 `AuthorizationDenied` 拒绝，research 节点直接崩，`tests/unit/agents/test_localizer.py` 因为用假 Gateway 桩从没测出来。已修复（commit `08b46c1`）：给 `_AGENT_PERMISSIONS` 加 `"localizer"` 条目 + 补权限单测 + 顺带修好一直没注册的 `git_blame` 工具。代码异味清单见"十、代码质量审计"。

## 二、现状核实：哪些说法名不副实

| 说法 | 核实结论 | 证据 |
|---|---|---|
| "多 agent 协作，9 个 agent 各管一段" | ❌ 名不副实。只有一个 `AgentRuntime`（一个 model + 一个 gateway），`build_agent_nodes(runtime)` 把它传给全部 9 个节点函数共用 | `agents/nodes.py:41-63` |
| "memory 存储层" | ❌ 名不副实。`InMemoryTaskRepository` 只是任务状态的乐观锁 CRUD 仓库，无跨任务学习 | `storage/memory.py` |
| "18 种 SearchKind 精准检索" | ⚠️ 过度设计但未解决核心问题。检索机制很重，但没有调用图/依赖图，本质是"加检索策略"而非"建代码结构关系" | `grep call_graph/dependency_graph` 全仓库零匹配 |
| "Localizer 定位循环" | ✅ 真实存在但工具集不完整。4 种动作 search/read/blame/finish，**没有 goto_definition/find_references** | `docs/interview-prep-complete.md` 1.1.5 |
| "Intake 结构化理解 Issue" | ⚠️ 信息源单一。只吃 issue title+body+number，不读 CI 日志/测试失败 stack trace | `domain/models.py` → `IssueSpec` |
| "推理范式比 ReAct 新" | ✅ 真实。Localizer 借鉴 LocAgent（图引导定位），StateGraph 的有界重试+evidence-driven fallback 接近 Reflexion 式验证-重试 | `docs/interview-prep-complete.md` Q17-Q18 |

## 三、四项核心改造

### 1. Issue 理解：复现优先（reproduce-first），不只是读日志
Research 之前先尝试跑现有测试/写最小复现脚本拿到真实 stack trace，用 trace 里的文件/行号/函数名直接作为定位起点，而不是纯靠语义检索去猜。CalibrationJudge 规则版可直接用 trace 信号做校准。

### 2. 依赖感知的代码导航：只做调用图+import图，不做完整 CPG
完整 Code Property Graph（含数据流分析）工作量过大，收窄为**调用图+import图**（tree-sitter 提取，跳过数据流），已覆盖"谁调用了这个函数""改这个文件影响哪些下游"两个最高频需求。
- 给 `Localizer` 加 `goto_definition`/`find_references` 两个动作
- 作为新 QueryKind（如 `GRAPH`）接入 `search/kind_mapping.py`，`symbol`/`definition`/`dependency` 几个 SearchKind 的主搜升级为图查询
- 顺便合并精简低频触发的 SearchKind（作者自己 Q11 已承认）

### 3. 多 Agent 编排：先做消融实验，不默认拆分更好
拆成 Locator/Coder/Critic 会引入"传话丢信息"和延迟成本，不一定优于"一个更长上下文的单 agent"。**先在同一批任务上跑单 agent vs 拆分角色两个版本**，比较 resolved rate/成本/延迟，用数据决定要不要拆。若数据支持拆分：Locator（只有代码导航工具，输出结构化定位假设）→ Coder（只接收定位结果，产出 patch）→ Critic（判断退回 Locator 还是 Coder），子 agent 间用结构化消息通信。

### 4. 记忆层：case-based 案例库，注意租户隔离
每次修复成功后存一条"bug 模式+定位路径+修复模式"，下次遇到相似模式优先复用。**必须按 `tenant_id` 隔离**——项目到处是租户隔离设计，这层不能破坏这个边界。现有 `InMemoryTaskRepository` 保留（职责是任务状态持久化，不改名不改动）。

## 四、其他模块的改进机会

5. **Verification 节点**：补充回归检测，复用第 2 点的调用图做 test impact analysis——patch 改了哪些函数，调用图告诉你哪些测试间接依赖，额外跑一遍
6. **Planning 的 `deterministic_risk`**：用调用图堵上 Q20 承认的盲区——"改 `src/auth.py` 不触发规则但改了密码验证逻辑"，升级为"改动函数是否能从认证/安全相关文件 N 跳可达"
7. **Review 节点前置静态检查**：LLM review 之前先跑 linter/type checker 做便宜的确定性预筛，复用 planning 节点"deterministic 规则+LLM 判断取更严格值"的既有设计哲学
8. **评测数据真实性**（优先级最高，见六）
9. **前端加"改造前后对比"视图**：挑一个旧版本处理错、新版本处理对的真实 case，两次定位过程并排可视化

## 五、优先级

1. 第 8 点（换真实评测数据）——最先做，否则后面改进的证据都立不住
2. 第 2 点（调用图）+ 第 6 点（堵已知风险盲区）——一套基建两处复用
3. 第 1 点（复现优先）
4. 第 3 点（多 agent 消融实验）——先只做单/双两版对比
5. 第 4、5、7、9 点作为时间富余时的加分项

## 六、评测方法论：跑真实 SWE-bench 之前

直接冲官方 SWE-bench 的问题：慢（每任务要起 Docker 环境）、贵、样本量大跑不起，且只看最终 resolved/not 看不出问题在搜索还是编码环节。

**分层指标**（每个任务同时记录）：
1. **定位准确率**：agent 定位的文件/函数与真实修复 commit 改动范围是否重合，不需要跑测试/生成 patch，最快最便宜
2. **生成成功率**：有没有产出语法合法、能应用的 patch
3. **验证通过率（resolved rate）**：最终指标，单独看诊断不出问题在哪层

**四步流程**：
1. 组件级单元测试（调用图构建、reproduce-first），用手写的小型合成仓库测试，不涉及 LLM
2. 自建真实但小的金标准集：从 1-2 个熟悉的开源仓库挑 10-15 个真实历史 bug（有修复 commit + 测试），**替换 `docs/evidence/` 里现在的 `swebench__test-N` 占位数据**
3. 在金标准集上做消融对比（改造前 vs 改造后），三层指标都记录，用 `significance.py` 的 `paired_bootstrap_delta` 做显著性检验
4. 确认小规模有效后，从 SWE-bench Lite/Verified 抽样 50-100 个真实 instance 做规模化验证——这是流程最后一步，不是第一步

## 七、风险对冲：如果修复不了真实仓库

真实公开数据参照：Devin 在 SWE-bench 约 14% resolved，早期 SWE-agent 类系统普遍 10-20%，顶级模型+精心 scaffolding 也就 50-70%（大团队长期打磨）。个人在有限时间内不需要、也不太可能做到"稳定修复真实仓库"。

对冲手段：
- 分层指标兜底——就算最终 resolved rate 没提升，定位准确率提升本身就是独立可交付成果
- 合成 bug 数据集保底——自己往代码里注入难度可控的 bug，保证能看到"从全错到部分修对"的进步曲线，与真实历史 bug 分层汇报
- 提前缩小范围——只处理单文件、有明确失败测试、错误类型明确（逻辑/边界条件）的 bug，不追求任意 issue 的通用修复
- 结果不理想时的汇报框架：**假设 → 怎么验证的（分层指标+显著性检验） → 结果和下一步**，而不是回避结果

## 八、明确排除的范围

- 完整重写 18 种 SearchKind 体系——精简+接入调用图即可
- 完整 Code Property Graph（含数据流分析）——收窄为调用图+import图
- AegisEvo（Rust 进化优化引擎）——配套项目，不纳入本次改造
- Inspect Bridge 完整接线——README 已说明是骨架状态
- 完整强化学习训练管线

## 九、重新对话恢复流程

每次重新对话时：
1. 读取此文档了解改造计划全貌
2. 读取 `架构记忆文档.md` 恢复现有系统的详细架构上下文
3. 如需查看现有任务状态，检查 `output/` 目录
4. 按五、优先级顺序推进，每完成一项在此文档打勾/补充结果

## 十、代码质量审计（2026-09-11，自动化 code-smell 扫描）

不是死代码（另一轮扫描已确认无死代码/无孤儿脚本），是"能跑但会被 code review 挑出来"的问题，按影响排序，尚未处理：

1. `agents/nodes.py` 的 `coding()` 是约 300 行的巨函数（上下文收集循环 + patch 生成重试循环 + 未声明路径治理检查 + 产物写入 + 内联校准，全挤在一个闭包里）——应拆成 `_gather_context()` / `_propose_and_apply_patch()` / `_run_calibration(stage)`。
2. `domain/ports.py` 的 `ModelPort.structured()` 协议签名过时：真实实现和所有调用方都传了 `max_attempts`，port 定义里没有，port 已经不能代表真实契约——补上 `max_attempts: int = 3`。
3. `agents/nodes.py` 里 `ToolCall(task_id=..., tenant_id=..., repo_id=..., commit_sha=...)` 这段样板在 research/coding/review/pr 里重复了 14 次——加一个工厂方法或闭包封装。
4. `agents/nodes.py` 的 `CalibrationJudge` 调用块（本地 import + 实例化 + calibrate + 合并进 task_spec）在 research/planning/coding 三处逐字复制——抽成 `_apply_calibration(runtime, task, stage)`。
5. 全 `src/` 没有一处用 `logging` 模块；`worker.py` 生产失败路径用 `traceback.print_exc()` 打到 stdout，没有结构化记录和 tenant/task 关联；`query_rewriter.py`/`localizer.py`/`calibration.py`/`search/reranker.py`/`search/adapters/opensearch.py` 里"降级为规则版"的 `except Exception` 分支也都没记录真正捕获到的异常，导致"真 bug"和"正常降级"在生产日志里长得一模一样——引入 `logging.getLogger(__name__)`，降级前先 `warning` 记录原始异常。
6. 不确定，需要再看一遍：`evaluation/swebench_runner.py` 的 evidence-driven fallback 逻辑手写重复了一遍 `graph/routes.py` 的 `route_after_review`，两份实现已经出现分歧（SWE-bench 版跳过了 `verification.passed` 检查）——可能是有意为之（官方 harness 就是 verifier），但重复的业务规则是漂移风险，建议抽到 `policies/risk.py` 共用。
7. 次要：`cli.py` 的 `_decimal`/`_nonnegative_decimal` 应合并成一个带 `allow_zero` 参数的函数。
8. 不确定，优先级低：`storage/sql.py`（471 行）一个文件装了 task repo / queue / operation log / task completion 四类职责，各个类本身还算内聚，值不值得拆看这个文件后续还长不长。
